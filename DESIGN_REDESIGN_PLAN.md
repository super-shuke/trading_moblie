# GeoTravel 当前设计总结与改版整改方案

> 文档状态：改版讨论稿  
> 分析依据：当前 Flutter 项目代码（2026-07-03）  
> 目的：在修改整体设计前，统一产品定位、信息架构、页面职责与整改优先级。

## 1. 结论先行

当前项目已经形成“地球探索 → 城市 → 地点 → Tips / 导航”的基础体验，视觉上也建立了星空地球、自然色和旅行杂志感。但产品主线尚未闭环：用户可以探索和收藏，却无法自然地把地点变成一趟可管理的旅行。

当前 TabBar 中的 **Itinerary 方向不正确**。它直接展示某个固定日期、固定城市的单日停靠点拖拽列表，本质上是“行程详情中的编辑模块”，不应该承担一级 Tab 根页面的职责。

建议将整体产品主线调整为：

**Explore（发现灵感） → Trips / 行程（组织与执行旅行） → Profile（个人沉淀）**

其中：

- TabBar 第二项建议显示为 `Trips`（中文为“行程”），作为全部旅行的管理中心。
- `Itinerary` 保留为一趟旅行内部的“每日计划 / 路线详情”，下沉到二级页面。
- Explore 中的城市、POI、收藏应能直接进入“加入行程”流程，补齐核心闭环。

---

## 2. 当前产品定位与体验特征

### 2.1 当前可感知定位

GeoTravel 是一款以 3D 地球为主要视觉入口的旅行探索应用，核心内容包括：

- 基于当前位置和地球视图发现城市；
- 浏览城市与 POI；
- 查看地点的多维评价和本地 Tips；
- 收藏 POI；
- 查看简单的单日行程并调整地点顺序；
- 查看个人旅行统计和已收藏地点。

### 2.2 当前设计语言

- **品牌视觉**：星空、地球、自然与探索感，首页沉浸感较强。
- **色彩**：浅色为米白背景，深色为墨绿黑背景，亮绿色为主强调色，橙色和琥珀色为辅助色。
- **形态**：圆角卡片、胶囊标签、细边框、浮动毛玻璃底部导航。
- **排版**：大标题搭配高字距全大写标签，偏旅行杂志 / Atlas 风格。
- **主题能力**：已具备 Light / Dark Theme 和自定义 Design Token。
- **动效**：Tab 使用淡入淡出，详情页使用横向滑动；地球支持自动旋转和独立 Demo。

### 2.3 当前数据与技术状态

- 页面框架：Flutter + Material 3。
- 导航：`go_router` + `StatefulShellRoute.indexedStack`，三个 Tab 状态可保留。
- 共享状态：`ChangeNotifier + InheritedNotifier`。
- 内容数据：城市、POI、Tips、Itinerary 和 Profile 主要来自 Mock Data。
- 已接入能力：定位、登录、HTTP、WebSocket、Unity / Flutter 地球组件。
- 当前行程模型只支持一个 `Itinerary`，不支持多行程、多日、状态或协作者。

---

## 3. 当前页面与导航结构

```text
启动
└── Login 登录页
    ├── Google 登录 / 游客模式
    ├── Globe Demo
    └── 主应用
        ├── Tab 1: Explore
        │   ├── 首页：位置 + 3D 地球 + Hot Locations
        │   ├── City Details 城市详情
        │   │   └── POI Details 地点详情
        │   │       ├── Tips 列表
        │   │       └── Navigation 导航页
        │   └── Globe Demo（独立全屏页）
        ├── Tab 2: Itinerary
        │   └── 固定单日行程 + Stop 拖拽排序
        └── Tab 3: Profile
            ├── 用户信息
            ├── 已访问城市地球
            ├── 旅行统计
            └── Saved POI
```

### 3.1 一级页面职责

| 页面 | 当前职责 | 主要内容 | 当前问题 |
| --- | --- | --- | --- |
| Explore | 发现入口 | 地球、当前位置、热门城市 | 有视觉记忆点，但搜索、筛选和完整城市列表入口不足 |
| Itinerary | 单日计划编辑 | 日期、标题、Stop 列表、拖拽排序 | 页面层级错误；缺少行程列表、创建流程、多日结构和空状态 |
| Profile | 个人数据 | 统计、访问地球、收藏 | 与 Explore 重复展示地球；设置、账号和旅行沉淀能力不足 |

### 3.2 二级页面职责

| 页面 | 当前职责 | 已有交互 | 缺口 |
| --- | --- | --- | --- |
| City Details | 城市介绍和地点列表 | 进入 POI | 城市收藏按钮为空；缺少筛选、地图/列表切换、创建行程入口 |
| POI Details | 地点决策 | 收藏、Tips、导航 | 缺少“加入行程”；真实图片、营业信息、距离等决策信息不足 |
| Tips | 本地建议 | All / Tips / Avoid 筛选 | 缺少发布、排序、举报等后续能力 |
| Navigation | 到达 POI | 模拟路线和到达 | 当前为占位地图与固定文案，不是实际导航能力 |
| Login | 身份入口 | Google、游客、Demo | 视觉和主应用品牌需进一步统一 |

---

## 4. 现有设计的主要问题

### 4.1 产品链路未闭环

当前链路止于“浏览 / 收藏 / 导航”。收藏地点只能在 Profile 查看，不能直接组织成旅行；Explore 与 Itinerary 之间没有动作连接。这会导致第二个 Tab 像独立 Demo，而不是产品主流程的一部分。

### 4.2 首页强调形式，但发现效率不足

3D 地球是差异化资产，应该保留；但目前地球禁用了手势，Hot Locations 只有少量横向卡片，“SEE ALL” 又跳回当前 `/explore`，用户无法真正搜索或系统浏览目的地。

建议把地球定义为“灵感入口”，同时补充高效率入口：搜索、附近、主题集合、最近浏览和完整目的地列表。

### 4.3 信息层级和视觉一致性不足

- Explore 使用星空深色背景，而 Itinerary / Profile 使用普通主题背景，三个一级页像不同产品。
- 多处图标颜色硬编码为 `Colors.white38/54`，在浅色主题中对比度不稳定。
- 页面普遍使用英文全大写小字号标签，品牌感明确，但信息密集时可读性偏弱。
- Profile 作为 Tab 根页仍显示返回按钮，一级导航语义不一致。
- City / POI 图片目前是渐变占位图，无法支撑旅行内容的情绪价值和决策价值。
- 毛玻璃 Bottom Bar 有明确风格，但需验证内容是否被 `extendBody` 遮挡以及不同平台的对比度。

### 4.4 交互反馈与状态不完整

- 缺少 Skeleton、网络错误、无数据、无收藏、无行程等状态。
- 城市收藏按钮为空实现。
- 行程只有拖拽排序，没有添加、删除、编辑时间、冲突提示、地图预览或保存反馈。
- Navigation 当前是视觉占位，不应在产品文案中暗示真实导航已可用。
- 多语言基础配置存在，但页面文案仍大量硬编码英文。

---

## 5. TabBar 的 Itinerary 方向问题

### 5.1 为什么当前方向不对

当前 Tab 根页直接读取唯一的 `store.itinerary`，展示：

- 一个固定日期；
- 一个固定标题；
- 固定数量的 stops；
- 唯一核心操作“拖拽排序”。

它存在四个根本问题：

1. **抽象层级过低**：一级 Tab 应承载一类长期任务，而当前页面只是某一天的编辑器。
2. **没有入口逻辑**：用户没选择城市、日期或行程，进入 Tab 就看到一份默认计划。
3. **无法覆盖旅行生命周期**：没有即将出发、计划中、旅行中、已结束等状态。
4. **与 Explore 断开**：收藏和查看过的 POI 无法加入行程，行程也无法反向打开地点。

### 5.2 推荐方向：Trips 行程中心

第二个 Tab 的产品语义建议从“编辑 itinerary”升级为“管理 trips”。

建议命名：

- 英文 Tab：`Trips`，比 `Itinerary` 更短、更适合一级导航，也能涵盖多趟旅行。
- 中文 Tab：`行程`。
- `Itinerary` 用于旅行内部的每日安排页面或数据对象。

### 5.3 Trips 根页建议结构

```text
Trips / 行程
├── 顶部标题 + 新建行程按钮
├── Upcoming 即将出发
│   └── 主行程卡：目的地、日期、倒计时、同行者、完成度
├── Planning 计划中
│   └── 草稿行程卡
├── Past Trips 过去行程
│   └── 旅行回顾卡
└── 空状态
    ├── 从目的地开始探索
    └── 创建第一趟旅行
```

### 5.4 Trip 详情建议结构

```text
Trip Detail
├── Hero：城市 / 国家 / 日期 / 天气摘要
├── Overview：天数、地点数、同行者、完成度
├── Day 切换：Day 1 / Day 2 / Day 3
├── 当日时间线 Itinerary
│   ├── Stop 卡片
│   ├── 路程与交通段
│   ├── 时间冲突 / 营业时间提醒
│   └── 拖拽排序
├── Map 地图视图
├── Saved for this trip 候选地点
└── 操作：添加地点、编辑日期、分享 / 协作
```

### 5.5 Stop 卡片整改

当前 Stop 卡只有序号、时间、名称、备注和拖拽图标。建议增加：

- POI 缩略图和类别；
- 到达时间、停留时长、营业状态；
- 与上一站的距离、交通方式和预计耗时；
- 时间冲突或路线绕行提示；
- 点击进入 POI 详情；
- 更多菜单：编辑、移到其他日期、删除；
- 拖拽只在编辑模式显示，避免把排序误当成页面唯一任务。

### 5.6 Explore 与 Trips 的闭环

```text
Explore 城市 / POI
        ↓ 收藏或“加入行程”
选择已有 Trip / 新建 Trip
        ↓
选择日期与时间（也可先放入候选清单）
        ↓
Trip Detail 自动形成每日路线
        ↓
旅行中进入 Today 模式并逐站执行
        ↓
旅行结束后沉淀到 Profile
```

---

## 6. 推荐的新信息架构

### 6.1 一级导航

| Tab | 核心问题 | 页面目标 | 首要 CTA |
| --- | --- | --- | --- |
| Explore / 探索 | 我想去哪里？ | 发现城市和地点 | 搜索 / 查看城市 / 加入行程 |
| Trips / 行程 | 我要如何规划和执行？ | 管理全部旅行 | 新建行程 / 继续规划 |
| Profile / 我的 | 我积累了什么？ | 管理身份、收藏和旅行记录 | 查看记录 / 设置 |

建议保持三个 Tab，不增加第四个 Tab。搜索属于 Explore，收藏可作为 Profile 的内容分区，也可在加入行程时调用，不需要单独占一级入口。

### 6.2 推荐路由结构

```text
/explore
/explore/search
/explore/city/:cityId
/explore/city/:cityId/poi/:poiId
/explore/city/:cityId/poi/:poiId/tips

/trips
/trips/new
/trips/:tripId
/trips/:tripId/day/:dayId
/trips/:tripId/map

/profile
/profile/saved
/profile/history
/profile/settings

/navigate/:poiId      # 仅在真实导航能力成立后保留该命名
/login
```

---

## 7. 各页面优化建议

### 7.1 Explore

- 保留地球作为首屏品牌资产，但允许旋转、缩放和点击标记。
- 顶部增加明确搜索入口，不用小字号文字链接承担主要导航。
- 将“Hot Locations”调整为可解释的内容模块，如“附近”“本月适合”“海滨城市”。
- 修复 `SEE ALL` 指向当前页面的问题，进入搜索 / 目的地列表或展开完整列表。
- 用户点击城市标记时先出现轻量预览卡，再进入详情，降低跳转成本。
- 地球加载失败时提供静态地图或城市列表降级，不让核心入口空白。

### 7.2 City Details

- Hero 下增加“加入行程”“保存城市”两个清晰动作。
- POI 支持分类筛选、排序以及地图 / 列表切换。
- 展示最佳旅行月份、建议停留天数、当前天气等决策信息。
- 城市页的 Bookmark 必须完成业务实现，或在 MVP 中暂时移除。

### 7.3 POI Details

- 主 CTA 调整为“加入行程”，次 CTA 为“保存”；“导航”在临近或旅行中场景再突出。
- 补充地址、营业时间、距离、价格、预计停留时长和外部地图入口。
- Tips 是差异化内容，应在详情页预览 2–3 条，而不是只显示按钮。
- 收藏后提供 Snackbar，并允许立即“加入行程”。

### 7.4 Trips

- 根页改为多行程列表，不再直接进入单日时间线。
- 创建行程最少只要求目的地和日期；其余信息可渐进补充。
- 区分候选地点和已排入日程的地点，降低早期规划压力。
- 旅行当天提供 Today 模式：下一站、路线、离开提醒、离线信息。
- 没有行程时使用有指向性的空状态，而不是填充默认 Mock 行程。

### 7.5 Profile

- 移除一级 Tab 页面上的返回按钮。
- 信息结构调整为：身份卡 → 旅行统计 → 旅行足迹 → 收藏 / 历史 → 设置。
- Profile 地球建议弱化为统计可视化，避免和 Explore 主地球竞争视觉中心。
- 增加登录状态、语言、主题、隐私与定位权限入口。

---

## 8. 视觉系统整改建议

### 8.1 统一整体气质

建议延续“现代旅行 Atlas”方向，不推翻现有地球和自然色资产。三个 Tab 应共享同一套材质、间距、标题结构和背景逻辑：

- Explore 可以更沉浸，但其卡片和标题系统应与 Trips / Profile 一致。
- 浅色与深色主题都必须通过语义 Token 取色，禁止业务组件硬编码白色透明度。
- 真实旅行图片应成为城市和 POI 的主要内容资产，渐变占位只用于加载状态。

### 8.2 Design Token 补全

现有 `AppCommon` 已有颜色、圆角和间距，建议继续补充：

- `surfaceGlass`、`surfaceOverlay`、`textOnImage`；
- `success`、`warning`、`info`、`disabled`；
- 卡片阴影等级；
- Bottom Bar 高度和页面底部安全间距；
- 图片比例（Hero、列表、缩略图）；
- 标准触控尺寸（至少 44×44）；
- 动效时长与曲线。

### 8.3 字体与文案

- 大标题可保留当前编辑感，正文优先保证长文本可读性。
- 全大写高字距标签仅用于短分类，不用于主要操作或长文案。
- 确定产品主语言策略；所有用户可见文案进入 ARB，不再直接写在 Widget 中。
- 统一术语：Explore / Trips / Profile；Trip / Day / Stop；Saved / Add to trip。

---

## 9. 数据模型与技术整改

当前单例 `Itinerary` 模型不足以支持目标体验。建议调整为：

```text
Trip
├── id
├── title
├── destinationIds[]
├── startDate / endDate
├── status: draft / upcoming / active / completed
├── coverImage
├── travelers[]
├── days[]
└── savedPoiIds[]

TripDay
├── id
├── date
├── title
└── stops[]

TripStop
├── id
├── poiId
├── startTime
├── duration
├── transportToNext
├── note
└── order
```

状态层建议从单个 `_itinerary` 改为：

- `List<Trip> trips`；
- `activeTripId`；
- Trip 的增删改查；
- Day 和 Stop 的增删改查与跨日移动；
- 收藏 POI 与 Trip 候选池的关联；
- 持久化、加载、错误和同步状态。

路由和 Widget 文件也建议采用小写 snake_case，并按 Feature 组织，避免继续使用 `Itinerary.dart`、`Profile.dart` 这类不符合 Dart 规范的文件名。

---

## 10. 分阶段整改计划

### P0：先纠正产品结构

目标：让主链路成立，避免继续在错误页面层级上堆功能。

- Tab 文案从 `Itinerary` 改为 `Trips` / “行程”。
- 创建 Trips 根页：Upcoming、Planning、Past、空状态。
- 将现有拖拽列表迁移为 Trip Detail 下的 Day Itinerary。
- 支持新建行程、添加 / 删除 Stop、从 POI 加入行程。
- Profile 移除返回按钮。
- 修复 Explore `SEE ALL` 无效入口与 City 收藏空实现。
- 定义并落地新的 Trip / TripDay / TripStop 数据模型。

### P1：提升可用性和一致性

- Explore 增加搜索、分类与地球交互降级方案。
- Trip Detail 增加 Day 切换、地图视图、候选地点和路线段。
- 完成空态、加载态、错误态、反馈提示和删除确认。
- 清理硬编码颜色与硬编码文案，完成主题和国际化一致性。
- 统一一级页面 Header、卡片、按钮和底部安全区。
- 增加关键 Widget Test 和核心流程 Integration Test。

### P2：形成差异化能力

- 自动路线优化与时间冲突提示。
- 多人协作、分享、评论和变更同步。
- Today 模式、离线内容、真实地图 / 外部导航。
- 旅行结束后的回顾、足迹与内容沉淀。
- 根据真实行为数据优化推荐和首页内容模块。

---

## 11. 改版验收标准

### 产品闭环

- 用户能从 Explore 找到 POI，并在 3 步内加入一趟 Trip。
- 用户第一次进入 Trips 时不会无缘无故看到默认行程。
- 用户能创建多趟旅行，并区分计划中、即将出发和已结束。
- 用户能在 Trip 内管理多日和 Stop，而不是只能拖拽一个固定列表。

### 体验一致性

- 三个一级 Tab 的 Header、边距、卡片和 Bottom Bar 行为一致。
- Light / Dark 模式下不存在依赖 `Colors.whiteXX` 导致的低对比度文本或图标。
- 一级 Tab 根页不出现返回按钮。
- 所有关键操作都有加载、成功、失败、空状态和撤销 / 确认反馈。

### 工程质量

- 新 Trip 模型和状态操作有单元测试。
- Explore → Add to Trip → Trip Detail 有集成测试。
- Trips 空状态、多行程列表、Day 切换和 Stop 编辑有 Widget Test。
- 用户可见文案全部通过本地化资源管理。

---

## 12. 本次改版建议保留与暂缓的内容

### 建议保留

- 3D 地球作为品牌核心视觉；
- 三 Tab 总体框架；
- 自然色和 Atlas 编辑感；
- City → POI → Tips 的内容层级；
- Theme Token、`go_router` 和共享 Travel Store 的基础结构。

### 建议暂缓

- 在行程基础闭环完成前继续扩展 Globe Demo 控制项；
- 将占位 Navigation 包装成真实导航能力；
- 过早加入复杂社交功能；
- 仅做视觉换肤而不调整 Itinerary 的页面层级和数据模型。

最终优先级应是：**先改信息架构和核心任务，再统一视觉，最后增加智能路线与协作能力。**
